# Required in production: absolute path on a persistent, private disk (outside public/).
# Back up this directory and restrict access to authorized Movo operators.
MOVO_DATA_DIR=
# Optional: existing notification integration; receives JSON with recipient and intent.
MOVO_ENQUIRY_WEBHOOK_URL=
MOVO_ENQUIRY_WEBHOOK_TOKEN=
# Supply the actual existing community invite. No community button is shown until set.
NEXT_PUBLIC_GIVEAWAY_DISCORD_URL=
NEXT_PUBLIC_SITE_URL=https://www.movotechnologies.com
